#include <stdio.h>
#include <time.h>

**
 * main - "A program that prints all possible different combinations of two digits"
 * Return: Always 0 (Success)
 */

int main(void)

{
	int d, p;

	for (d = '0'; d < '9'; d++)
	{

	for (p = d + 1; p <= '9'; p++)

	}
